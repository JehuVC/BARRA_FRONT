<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
  </PropertyGroup>

  <ItemGroup>
    <!-- Solo para los DTO de salida: reusan ResBase/Clase_Error de Core en
         vez de duplicar el contrato "resultado/error". Core no depende de
         DTO, asi que esto no crea un ciclo. -->
    <ProjectReference Include="..\Core\Core.csproj" />
  </ItemGroup>

</Project>
